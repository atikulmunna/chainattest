# Circuit Notes

This directory is reserved for witness-generation helpers, setup scripts, and benchmark tooling.

Immediate additions to implement:

1. `build_semantic_witness.*`
2. `build_eval_witness.*`
3. trusted setup helpers for `sem-v1` and `eval-v1`
4. benchmark runner for proof time and constraint counts

